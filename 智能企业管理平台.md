责任描述：
    负责前后端的搭建，数据格式的定义，页面、接口、路由，前后端的联调
    负责基于LangChain Agent框架实现AI对话式操作核心模块，支持自然语言发起审批、请假、查询数据等企业操作
    负责NL2SQL智能数据查询引擎的开发，基于LangChain + 大模型将用户自然语言转换为Oracle SQL，实现对话式数据报表查询
    负责审批流程引擎的设计与开发，基于Activiti7实现多级审批、会签、条件分支等复杂工作流
    负责基于RAG的企业知识库问答系统，员工可通过对话查询公司制度、HR政策、操作手册等文档
    负责考勤打卡模块的开发，包括GPS/WiFi定位打卡、加班统计、异常考勤处理
    负责人事管理模块，包括组织架构树形管理、员工档案、入离职流程、薪资核算
    负责RBAC权限模型的设计，基于SpringSecurity + JWT实现菜单/按钮/数据三级权限控制
    负责数据看板的可视化开发，基于ECharts实现经营数据、人事数据、考勤数据多维度报表
    负责AI会议纪要自动生成功能，基于大模型对会议录音转文字后自动提取摘要、待办事项、决议结论

项目简介： 创达智管是创达公司自主研发的AI驱动型企业内部管理平台，覆盖OA办公、人事管理、审批流程、考勤薪资、数据报表等企业核心管理场景。系统最大亮点在于深度融合AI Agent能力，员工可通过自然语言对话完成请假申请、报销提交、数据查询、流程创建等日常操作，彻底改变传统"表单填写+逐级审批"的繁琐模式。底层基于LangChain Agent框架实现Tool Calling机制，结合NL2SQL技术将自然语言转换为Oracle SQL进行数据查询，并通过RAG检索增强生成技术构建企业知识库智能问答，为企业管理赋予"对话即操作、提问即答案"的智能化体验。
应用技术： Vue3、Element Plus、Pinia、Vue Router、Axios、ECharts、MyBatisPlus、Activiti7（工作流引擎）、Oracle 19c、Redis、RabbitMQ、LangChain、LangChain4j、ChatGLM/Qwen大模型、RAG（检索增强生成）、NL2SQL、FAISS向量数据库、HuggingFace Embedding模型、MinIO、Docker、Nginx、EasyExcel
项目模块：
    AI对话中心（核心模块）： 基于LangChain Agent + Function Calling实现对话式企业操作，支持自然语言请假（"帮我请明天的年假"）、对话报销（"提交上周出差的500元餐费报销"）、对话查数据（"本月研发部加班最多的是谁"）、对话创建流程（"发起一个采购申请流程"）
    NL2SQL智能查询模块： 基于LangChain + 大模型将自然语言转为Oracle SQL，支持"本季度销售额对比上季度增长多少"等复杂查询，结果自动生成可视化图表
    企业知识库RAG模块： 企业制度、HR政策、操作手册等文档向量化存储，员工对话式查询（"年假最多能请几天"、"报销流程怎么走"），基于RAG检索+大模型生成精准回答
    审批流程管理模块： 基于Activiti7工作流引擎，支持请假、报销、出差、采购、合同等多类型审批，多级审批、会签、条件分支、催办撤回
    考勤打卡模块： GPS/WiFi定位打卡、上下班签到、加班申请与统计、迟到早退异常处理、月度考勤汇总
    人事管理模块： 组织架构管理（部门/岗位树形结构）、员工档案CRUD、入离职流转、合同管理、人员调动
    薪资管理模块： 工资核算、社保公积金、个税计算、工资条生成与推送
    绩效考核模块： KPI/OKR考核方案配置、考核周期管理、评分与排名、绩效报表
    招聘管理模块： 职位发布、简历管理、面试安排、录用流程、人才库
    日程/会议管理模块： 会议室预定、日程创建、会议纪要AI自动生成（录音转文字+大模型摘要提取）
    公告/通知模块： 企业公告发布、消息推送（站内信/邮件/钉钉）、已读未读统计
    任务/工单管理模块： 任务创建与分配、进度跟踪（看板视图）、协作评论、超时预警
    数据看板模块： 经营数据、人事数据、考勤数据、审批数据多维度ECharts可视化大屏，AI智能分析趋势与异常
    日志审计模块： 操作日志记录、登录日志、敏感操作审计、数据变更追踪

项目难点分析
    1. LangChain Agent对话式操作引擎（核心难点）
        基于LangChain的Agent框架（ReAct模式）构建企业操作Agent，定义多个Tool（请假Tool、报销Tool、查数据Tool、创建流程Tool等），大模型根据用户意图自主决策调用哪个Tool
        难点在于意图识别的准确性（"请假"vs"查请假记录"需要区分），多轮对话中的上下文管理（用户说"帮我请假" -> "从周一到周三" -> "年假"，需要跨轮次收集参数），以及Tool调用的参数校验与异常兜底
        需要设计对话状态机，当AI采集到的参数不完整时（缺少日期、假种等），主动追问用户补全信息，而不是直接报错

    2. NL2SQL自然语言转Oracle SQL
        用户用自然语言描述查询需求，LangChain将其转换为可执行的Oracle SQL语句，涉及表结构感知（将Oracle数据字典注入Prompt）、多表关联推理、聚合函数生成
        难点在于Oracle SQL方言的适配（分页用ROWNUM而非LIMIT、日期函数差异等），复杂查询的准确率（多表JOIN + GROUP BY + HAVING），以及SQL注入防护（生成的SQL必须经过白名单校验与权限过滤，防止越权查询）
        需要建立Schema元数据缓存机制，将表名、字段名、中文注释映射关系预加载，降低每次查询的Token消耗

    3. RAG企业知识库的精准检索
        企业文档格式多样（PDF/Word/Excel/PPT），需要统一解析后进行文本分块、Embedding向量化，存入FAISS向量数据库
        难点在于分块策略对检索质量的影响（公司制度类文档的章节层级结构需保留），检索时的相似度阈值调优（避免返回不相关内容），以及多文档来源的权重排序
        需要实现混合检索策略（向量检索 + BM25关键词检索融合），提升召回率与准确率

    4. Activiti7工作流引擎与动态表单
        基于Activiti7实现可视化流程设计器，支持拖拽式配置审批节点、条件分支（金额>5000自动加审）、会签（多人同时审批）、委托转办
        难点在于动态表单的设计（不同审批类型表单字段不同，需要JSON Schema动态渲染），流程运行时的状态管理，以及与AI Agent的打通（对话发起的审批需要自动匹配流程定义并启动流程实例）

    5. Oracle数据库性能优化
        企业管理系统涉及大量关联查询（组织架构树形递归查、考勤月度汇总、薪资批量核算），Oracle下需要利用CTE递归查询、分区表、物化视图等特性优化性能
        难点在于考勤和薪资模块的月末批量计算场景，数据量大时需要合理设计索引策略与批处理方案，避免锁表

    6. 多端消息推送与实时通知
        审批流转、任务分配、公告通知等场景需要实时推送，基于WebSocket + RabbitMQ实现消息的实时下发，支持站内信、邮件、钉钉/企微多通道
        难点在于消息的可靠投递（重试机制+消息状态表）、已读未读状态同步、以及高并发下的消息队列背压处理

    7. RBAC权限与数据隔离
        基于SpringSecurity + JWT实现菜单权限、按钮权限、数据权限三级控制，不同角色看到不同菜单，同一页面不同操作按钮，同一接口返回不同范围的数据
        难点在于数据权限的SQL拦截实现（MyBatisPlus拦截器动态注入部门过滤条件），以及AI对话查数据时也需要遵循数据权限（用户只能查到自己权限范围内的数据）

    8. AI会议纪要自动生成
        上传会议录音后，通过语音转文字（ASR）得到会议文本，再利用大模型进行结构化摘要提取：会议主题、参会人员、讨论要点、待办事项、决议结论
        难点在于长音频的分段处理（超出大模型上下文窗口），多人发言的角色区分，以及摘要的结构化输出格式控制（利用LangChain OutputParser）


做这个项目的要求：
重点突出ai相关的的东西。
数据库和缓存之类的全部使用轻量的。
还要涉及到多线程，锁，高并发之类的东西。
要用合适的设计模式。
三方工具全部改成轻量化的，比如数据库和缓存，我本地只有vue和python，没有下载别的三方工具。我的重点就是有一个AI相关的项目。而不是这些三方工具
项目要适配我本地的vue和python版本。
代码注释一定要写详细。然后帮我启动这个前后端项目。
给出一个文档告诉我手动启动需要如何操作。
尽可能的让这个项目符合实际的真实项目一些。
和AI相关的东西如果我本地没有，可以去下载，但是和AI无关的就最好不要下载了，但是下载的东西必须给我安装到D盘，并且把环境变量什么的都配置好，并且要把它以后的数据缓存也设置到D盘。
项目中要用真实调用大模型进行问答，就用我下边给的key和value，不要给我用假的回答

前后端项目都要，
前端项目放在D:\webStormProjects\workSpace06
后端项目放在D:\pyCharmProjects\workSpace03

我的大模型的key和value：llm = ChatOpenAI(
    model_name="gpt-4o-mini",
    base_url="https://api.openai-proxy.org/v1",
    api_key="sk-rbfyYt7GrXr9H3teqm56li0Jz8MsH0eRNhZs4MKZDH6inxKg",  
)。 这个可以直接写进项目和文档中，这是我自己的项目，没有安全性的考虑